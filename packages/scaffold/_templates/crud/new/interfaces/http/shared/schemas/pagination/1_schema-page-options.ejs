---
to: src/interfaces/http/shared/schemas/page-options.schema.ts
---
import { TSchema, Type } from '@sinclair/typebox'

export const PageOptions = Type.Object({
  page: Type.Integer({
    minimum: 1,
    maximum: 1000
  }),
  perPage: Type.Integer({
    minimum: 1,
    maximum: 100
  })
}, {
  $id: 'PageOptions',
  additionalProperties: false
})
