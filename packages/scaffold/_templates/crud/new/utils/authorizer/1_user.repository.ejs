---
to: src/utils/authorizer/user.repository.ts
unless_exists: true
---
import { User } from "./user";

export interface UserRepository {
  findById(id: string): Promise<User>
  findByEmail(email: string): Promise<User>
}